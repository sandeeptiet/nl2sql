apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-data
  namespace: nl2sql
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: crc-csi-hostpath-provisioner # CRC's actual storage class
