apiVersion: v1
kind: Service
metadata:
  name: mysql
  namespace: nl2sql
  labels:
    app: mysql
spec:
  type: ClusterIP
  clusterIP: None          # Headless — required for StatefulSet DNS
  ports:
    - port: 3306
      targetPort: 3306
      name: mysql
  selector:
    app: mysql
