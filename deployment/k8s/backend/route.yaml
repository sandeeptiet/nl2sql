apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: nl2sql-backend
  namespace: nl2sql
  labels:
    app: nl2sql-backend
spec:
  to:
    kind: Service
    name: nl2sql-backend
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
