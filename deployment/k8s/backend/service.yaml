apiVersion: v1
kind: Service
metadata:
  name: nl2sql-backend
  namespace: nl2sql
  labels:
    app: nl2sql-backend
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      name: http
  selector:
    app: nl2sql-backend
