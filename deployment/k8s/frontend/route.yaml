apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: nl2sql-frontend
  namespace: nl2sql
  labels:
    app: nl2sql-frontend
spec:
  to:
    kind: Service
    name: nl2sql-frontend
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
