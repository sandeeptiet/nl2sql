apiVersion: v1
kind: Service
metadata:
  name: nl2sql-frontend
  namespace: nl2sql
  labels:
    app: nl2sql-frontend
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: 3000
      name: http
  selector:
    app: nl2sql-frontend
